<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com" xmlns:viewmodels="clr-namespace:XamarinApp.ViewModels" x:DataType="viewmodels:SignUpPageViewModel"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="XamarinApp.Views.SignUpPage">
    <StackLayout VerticalOptions="CenterAndExpand" Padding="5">
        
        <Entry Placeholder="Email" Text= "{Binding UserName}"></Entry>
        <Entry Placeholder="Password" IsPassword="True"></Entry>
        <Entry Placeholder="Confirm Password" IsPassword="True" Text="{Binding PassWord}"></Entry>

        <Button Text="Sign Up" Command="{Binding SignUpCommand}" ></Button>
        <Label Text="Already have account? Sign In" TextColor="Blue"></Label>
    </StackLayout>
</ContentPage>